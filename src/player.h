#pragma once

namespace player {
    class Summoner {
        public:
            Summoner();
            ~Summoner();

            retrieve_match_history

        private:
            std::string puuid;
            std::string summoner_id;
            std::string account_id;
            std::string account_name;

    }
}
